#include <stdio.h>
int main()
{
  int a,count1,count2;

for(count1=1;count1<=3;count1++)
{
printf("\n >> first count is %d", count1);
  for(count2=1;count2<=3;count2++)
  {
    printf("\n second count is %d", count2);
  }
}

  return 0;
}
